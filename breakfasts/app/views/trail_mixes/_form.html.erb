<%= form_for(@trail_mix) do |f| %>
  <% if @trail_mix.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trail_mix.errors.count, "error") %> prohibited this trail_mix from being saved:</h2>

      <ul>
      <% @trail_mix.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ingredients %><br>
    <%= f.text_area :ingredients %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
